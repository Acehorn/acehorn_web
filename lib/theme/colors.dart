import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundDark = Color(0xFF0E0E10);
  static const Color gradientStart = Color(0xFF141E30);
  static const Color gradientEnd = Color(0xFF243B55);
  static const Color accent = Colors.deepPurpleAccent;
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color divider = Colors.white12;
}
